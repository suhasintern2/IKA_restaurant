# DECISIONS.md — locked decisions

> Source of truth. If any other doc disagrees with this file, the other doc
> is wrong and needs fixing. Every row here must be reflected in every spec
> it affects (mvp_spec / architecture / frontend_spec) in the same session
> it's added.

| Date | Decision | Reason | Files updated |
|------|----------|--------|----------------|
| —    | —        | —      | —              |
| 2026-09-21 | Use placeholder restaurant options: Restaurant A, Restaurant B, Restaurant C | The backend has not supplied canonical restaurant names yet | frontend_spec.md |
| 2026-09-21 | Upload each selected image in its own POST `/upload` request | Allows the required per-image pending, success, and failure states | frontend_spec.md |
| 2026-09-21 | Export CSV downloads the backend dataset returned by `/export` | The endpoint has no documented filter parameters | frontend_spec.md |
| 2026-09-21 | All backend routes live under the `/api` prefix; the frontend targets `/api` and the Vite dev server proxies `/api` to `http://localhost:8000` | Confirmed from `app/main.py`; the backend has no CORS, so the dev proxy is the bridge | frontend_spec.md |
| 2026-09-21 | `/upload` multipart field is singular `file` (one image per POST /upload request) | Confirmed from `app/api/upload.py`; the singular field name supersedes the earlier "`files`" assumption | frontend_spec.md |
| 2026-09-21 | Restaurant filter options are derived from the restaurant values `/entries` actually returns; the placeholders only show while the table is empty | The backend parses restaurant names from OCR, so static placeholder names would never match real rows. Supersedes the placeholder-options decision | frontend_spec.md |
| 2026-09-21 | `/export` supports the same `restaurant` + `ticket_number` filters as `/entries`; the UI exports whichever filtered view is currently applied | Confirmed from `app/api/export.py`. Supersedes the "exports the whole backend dataset" decision | frontend_spec.md |
| 2026-09-21 | The backend returns no `thumbnail` field and does not serve image bytes; the "Image" column shows the stored `image_filename`, and `extra_fields` are surfaced as a muted line under the Description cell | Confirmed from the `/entries` response shape; do not drop returned data silently | frontend_spec.md |
| 2026-09-21 | Frontend renders the backend `status` verbatim; all six statuses get CSS treatment: matched, needs_description, needs_ticket_number, needs_restaurant, needs_discrepancy_type, needs_review (the four "missing-field" ones share a review-style badge) | Backend statuses are fixed strings the UI must not rename | frontend_spec.md |
| 2026-09-21 | OCR keeps Tesseract `--oem 3 --psm 6` (line-oriented) with preprocessing: grayscale → 2x LANCZOS upscale when max(w,h) < 1400 → autocontrast(cutoff=1) → OSD deskew | PSM 6 keeps each bill line on its own OCR line, which the block segmenter depends on | ocr.py, architecture.md, mvp_spec.md |
| 2026-09-21 | OSD rotation is applied only when Orientation confidence >= 5.0 | Dense numeric tables (e.g. income reports) spuriously report `Rotate: 180` at low confidence; rotating them reverses the text and destroys OCR | ocr.py, architecture.md, mvp_spec.md |
| 2026-09-21 | Every OCR/correction to a parsed value is recorded in a `corrections` list (raw → value → reason); no silently "fixed" number | Auditability — nothing is guessed invisibly | segmentation.py, mvp_spec.md, architecture.md |
| 2026-09-21 | An entry's `discrepancy_type` comes from line-item markers ONLY; "Voids Total"-style summary lines go to the totals `summaries` block, never to markers/type | Otherwise every summary-containing report would falsely claim a discrepancy | segmentation.py, entry.py, mvp_spec.md |
| 2026-09-21 | Discrepancy→item association: marker binds to the nearest preceding item whose end line is < marker line; `high` confidence iff gap <= 2 lines and marker amount (when present) matches the item line_total; otherwise `low` | Proximity + amount equality are the only evidence available without semantic models | segmentation.py, mvp_spec.md |
| 2026-09-21 | Status precedence: needs_restaurant > needs_ticket_number > needs_discrepancy_type > needs_review > needs_description > matched; any `low`-confidence marker yields `needs_review` | The operator must review what the system couldn't confidently resolve — never guess | upload.py, mvp_spec.md |
| 2026-09-21 | Structured blocks (header/metadata/line_items/discrepancies/totals/payment) persist in a new additive `blocks` JSON column; raw `extracted_text` kept for audit; `/upload` response `parsed_data` stays the original 5 fields | Additive schema change keeps frontend and API contract stable | entry.py, db.py, entries.py, upload.py, architecture.md, frontend_spec.md |
| 2026-09-21 | Add `POST /upload/bulk` while preserving `POST /upload`; bulk uses multipart field `files`, accepts up to 30 images, and returns `{total, succeeded, failed}` with `{filename, reason}` for each failure | Preserves the frontend's existing single-image contract while enabling true 20-30 image requests and individual partial-failure reporting | upload.py, architecture.md, frontend_spec.md |
| 2026-09-21 | Process bulk images synchronously, one image at a time, through the existing upload pipeline; do not add polling in the MVP | Avoids introducing a job queue/status store and keeps extraction behavior identical; the frontend refreshes `/entries` after the request | upload.py, architecture.md, frontend_spec.md |
| 2026-09-21 | Ticket reconciliation uses a strict exact ticket number match first; fallback only to restaurant + date + amount when the numbers are absent or non-comparable, and it is surfaced as `Needs Review` not `Matched` | The real ticket format mixes full sales-report ticket numbers (`260919-58`) with short order numbers (`44`, `70`, `29`); false positives are worse than a conservative review state | reconciliation.py, mvp_spec.md, architecture.md, frontend_spec.md |
| 2026-09-21 | Handwriting is not treated as part of the standard printed OCR pipeline; it is stored as a separate `handwritten_note` field and reviewed as an operator-facing reason, not as a hidden parsed field | Printed receipt OCR and handwriting recognition are different problems; the note must be explicit and auditable | reconciliation.py, mvp_spec.md, architecture.md, frontend_spec.md |
| 2026-09-21 | Reason validity is not auto-approved; the system records `reason_provided` and `issue_addressed` with evidence text and leaves the final decision to the operator | The task explicitly requires operator review rather than a silent auto-approve flow | reconciliation.py, mvp_spec.md, architecture.md, frontend_spec.md |
| 2026-09-21 | Use `POST /api/upload/tickets` with multipart `files` for ticket screenshots; cap batches at 30 and report failures per screenshot | Ticket screenshots replace incomplete PDF exports while preserving partial success for real batches | upload.py, frontend_spec.md, architecture.md |
| 2026-09-21 | Normalize Ticket IDs from `#YYMMDD-N` to `YYMMDD-N` for the SQLite merge key, while returning the display form with `#` | The same Ticket ID appears on every screenshot and must produce one record regardless of OCR spacing or upload order | ticket.py, db.py, upload.py, mvp_spec.md |
| 2026-09-21 | Deduplicate ticket items by normalized item name + quantity + unit price; preserve `is_void: true` on parsed void rows | The same screenshot region may be uploaded twice, and item name alone is insufficient to distinguish repeated quantities/prices | ticket.py, db.py, mvp_spec.md |
| 2026-09-21 | Preserve populated ticket fields and merge non-empty `extra_fields`; never replace a value with a later blank | Screenshots are partial views of one ticket and may arrive in any order | db.py, mvp_spec.md, architecture.md |
| 2026-09-21 | Use `POST /api/upload/dockets` with multipart `files`, capped at 30, and return per-image failures | Docket photos need their own structured ingestion path and partial success behavior | upload.py, frontend_spec.md, architecture.md |
| 2026-09-21 | Store docket order numbers as `docket_order_no`, a weak trailing-number key, never as `ticket_id`; retain docket date/time for later tie-breaking | A docket's `49` can correspond to multiple full Ticket IDs on different dates, so treating it as a full ID would create false matches | docket.py, db.py, mvp_spec.md, architecture.md |
| 2026-09-21 | Keep printed OCR and `handwritten_reason` separate; the MVP uses best-effort OCR classification and exposes uncertainty through raw text and review flags | Printed and handwritten layers require different extraction handling, and no handwriting model is currently installed | docket.py, upload.py, frontend_spec.md, mvp_spec.md |
| 2026-09-21 | If multiple order numbers are detected in one docket photo, store the record with `review_required: true` instead of choosing a subject docket | Photos can contain partially framed neighboring dockets; guessing would attach the wrong discrepancy | docket.py, db.py, frontend_spec.md, mvp_spec.md |
| 2026-09-21 | Add `GET /api/reconcile/voids` and `/api/reconcile/voids/export`; backend owns matching and row order | Ticket void items and dockets must remain aligned identically in the UI and CSV | void_reconciliation.py, frontend_spec.md, architecture.md |
| 2026-09-21 | Match dockets to void items only by Ticket ID trailing digits; use docket date vs ticket date only to resolve exactly one collision | `docket_order_no` is a weak key and can collide across dates | void_reconciliation.py, mvp_spec.md, architecture.md |
| 2026-09-21 | Surface every unresolved collision candidate as `needs_review`; unmatched voids remain explicit `unmatched` rows; sort matched, review, unmatched | Operators must see missing explanations and ambiguity rather than receive a silent guess | void_reconciliation.py, frontend_spec.md, mvp_spec.md |
| 2026-09-21 | Export the exact reconciliation builder output and its `row_position` order | CSV must preserve the visual aligned order and not independently re-sort records | void_reconciliation.py, frontend_spec.md |
| 2026-09-21 | Add read-only `/api/tickets` and `/api/dockets` endpoints plus `/export` variants for parsed record tables | Upload responses alone are not a durable display contract; the frontend must render stored extracted fields and export them | records.py, frontend_spec.md, architecture.md |
| 2026-09-21 | Use three explicit frontend views: Tickets, Dockets, and Voids tally; Tickets flatten merged `items[]` into item rows | Ticket and docket schemas are different and must not be forced into one blank-heavy table | App.jsx, frontend_spec.md |
| 2026-09-21 | Backend owns Voids ordering through `row_position`; frontend renders it without client-side matching or resorting | The JSON tally and CSV export must remain identical, especially for collision and unmatched states | App.jsx, frontend_spec.md, void_reconciliation.py |
| 2026-09-21 | Detect ticket screenshots by full hyphenated Ticket ID plus ticket-screen labels before bill parsing; route them to `ticket_records` even through generic upload | Ticket and bill images are separate record types, and misclassification caused truncated IDs and garbage bill descriptions | upload.py, ticket.py, db.py, frontend_spec.md |
| 2026-09-21 | Preserve `YYMMDD-N` as one exact ticket identifier, with display form `#YYMMDD-N`; never split or truncate the hyphenated value | The second ID segment is the order identity needed for ticket/docket reconciliation | ticket.py, records.py, frontend_spec.md |
| 2026-09-22 | Use strict dedicated ticket and docket field schemas; expose parsed ticket IDs as `ticket_number` without the display `#`, flatten ticket `items[]` only in the Items UI cell, and keep docket handwriting separate from printed text | The two image types have different contracts and must not fall back to a generic raw-text display | ticket.py, docket.py, records.py, frontend_spec.md, mvp_spec.md, architecture.md |
| 2026-09-22 | Generic image upload rejects unrecognized non-ticket images; dockets must use `/api/upload/dockets` | Prevents a generic image path from silently inserting a docket row or mixing record types | upload.py, frontend_spec.md, architecture.md |
| 2026-09-22 | Upload endpoints verify the inserted row can be read back before reporting success | A stored file alone is not a successful structured upload | upload.py, db.py, frontend_spec.md |
| 2026-09-22 | Clear all old entries, tickets, dockets, and related rows before manual validation; use a normalized `ticket_items` child table and expose `ticket_id`, `order_number`, and `description` publicly | Old malformed data must not be migrated, and ticket line items need a separate nested schema | db.py, records.py, upload.py, frontend_spec.md, mvp_spec.md, architecture.md |
| 2026-09-22 | Keep parsed ticket and docket tables collapsed after upload until the matching Show data button is clicked | Operators should inspect the saved upload result before opening the dataset | App.jsx, frontend_spec.md |
| 2026-09-22 | Make complete hyphenated `ticket_id` the primary visible ticket field and expose the latest upload/merge time as `uploaded_at` | Ticket identity must never be visually lost, and operators need upload timing for audit | records.py, App.jsx, frontend_spec.md |